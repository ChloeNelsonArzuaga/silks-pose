
import cv2
import mediapipe as mp
import torch
import numpy as np

print("OpenCV:", cv2.__version__)  # Print OpenCV version to verify installation
print("MediaPipe:", mp.__version__)  # Print MediaPipe version to verify installation
print("Torch:", torch.__version__)  # Print PyTorch version to verify installation
print("NumPy:", np.__version__)  # Print NumPy version to verify installation

# This script checks that all required libraries are installed and prints their versions.
# Use it to quickly verify your Python environment for pose processing.
